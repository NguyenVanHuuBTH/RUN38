@model IEnumerable<HUMG.Models.H38>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.H38Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.H38Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.H38ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.H38ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.H38ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
